# Rules Register — 声明式规则索引

> 轻量索引，替代每次全量加载规则文件。
> LLM 默认只加载本文件，按 Router 触发词展开对应模块。

## Router 映射

| 触发词 | 展开文件 | Token 估算 |
|--------|---------|:--:|
| {触发词1} | {文件1} | ~{估算} |
| {触发词2} | {文件2} | ~{估算} |
| ... | ... | ... |

## 全局常驻规则

1. {规则1}
2. {规则2}
3. {规则3}

## 决策管道

```
[脚本预计算] → [DecisionState JSON] → [LLM 逻辑抉择] → [算子化输出] → [校验器]
```

脚本层做数值计算，LLM 层只做逻辑编排。
